# Polytoria-Math
A lightweight math utility library for Polytoria (which currently uses Lua 5.2).  PolyMath fills the gaps left by Lua’s primitive `math` library with common helpers used in games.  Simple drop-in module.
