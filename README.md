# PolyMath

A lightweight math utility library for **Polytoria** (Lua 5.2).

PolyMath fills the gaps left by Lua’s primitive `math` library with
common helpers used in games, animation, and simulation.

No globals. No engine dependencies. Drop-in module.

---

## Installation

1. Download `PolyMath.lua`
2. Place it in your project
3. Require it like any other module:

```lua
local PolyMath = require(path.to.PolyMath)
